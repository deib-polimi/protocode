<div class="control-content">
  {{#if title}}
    <span class="title">{{title}}</span>
  {{else}}
    <span class="placeholder">{{placeholder}}</span>
  {{/if}}
</div>
{{#link-to "controlEditText" this tagName="div" class="inner-control"}}{{/link-to}}